Level   

        - Level Boss
        - Group 1 - En1
                  - En2
                  - Group 2 - En3
                            - En4


handle groups of characters
- multiple players or NPCS can be grouped together
- groups can grow by incl other groups

2 types of objects:
NPC = NodeLeaf, and Group = Composite
- they both extend the same abstract class
- the composite class has a "has-a" relationship with the abstract class 
- if we get 2 interfaces at the test, we need to combine them to create the abstract class



2 interfaces which are being implemented by an abstract class
the abstract class is being extended by two regular classes
in the leaf class, everything that is not under the leaf interface will be implemented with:

        throw new UnsupportedOperationException();

Flyweight - only about reducing memory space for objects that share redundant data 
- FlyweightFactory contains the common (redundant) data
- In order to combine the common + distinct data for each object, the Flyweight interface will be used


Facade