SINGLETON req:
1. single shared match config used by all modules
2. objects must be created only when requested - lazy implementation
3. single shared file logger provided by the game server
4. logger needs to be initialized during application boot (ILogging) as one single object
5. unique loggers for ERROR, INFO, STATUS and GAMEPLAY - ENUM or HashMap<> 
6. loggers return the same logger interface and output file

