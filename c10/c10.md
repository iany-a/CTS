strategy pattern



\*NB: in JAVA you can call the same anonymous class code multiple times, the JVM will handle the logic.



Observer pattern = 1 observer watching the observable(S)

e.g.; used in onClick instances

keywords: "notified", "events", "subscribe / register"



C# - overloaded += operator for subscribing

JAVA - subscribe() / register()



OPT1:

UI --PULL NOTIFICATION--> SERVER (BACKEND) - on a timer (clock), risks overloading the server



OPT2: PUSH NOTIFICATIONS

BACKEND --INJECT (PUSH) NOTIFICATION-->UI - only on new events (Observer), no risk of overloading



web components e.g. web sockets, SSE





Diagram

Obs A, Obs B - > interface Observer //this observer interface will send the notification



lab test - obs - no time for handler, you can define an anonymous call to the method (e.g.; doClick())









